---
title: Planetary Playing Cards
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  <!-- Link to CSS -->
  <link rel="stylesheet" href="/styles/screen.css">
</head>
<body>
  <header>
    <h1>{{ title }}</h1>
  </header>

  <main class="planet-grid">
    <!-- Sun Card (left side, spanning two rows) -->
    <div class="sun-container">
      {% for planet in planets %}
        {% if planet.name == "Sun" %}
          <!-- Render Sun Card -->
          {% include "planet-card.liquid" with planet %}
        {% endif %}
      {% endfor %}
    </div>

    <!-- Planet Cards (remaining 8 sections) -->
    <div class="planet-row">
      {% for planet in planets %}
        {% if planet.name != "Sun" %}
          <!-- Render Planet Cards -->
          {% include "planet-card.liquid" with planet %}
        {% endif %}
      {% endfor %}
    </div>
  </main>
</body>
</html>
